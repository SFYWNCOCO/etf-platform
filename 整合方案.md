# ETF 系统整合方案

## 当前状态

### 两套代码并行

| 领域 | etf_system/ (旧) | etf-platform/ (新) | 问题 |
|------|:----------------:|:-----------------:|------|
| 穿透分析 | penetration_v7.py (824行) + v9.py (1019行) | pipeline.py (47行) + _v9_adapter.py | 旧：配置靠hardcode；新：适配器调用旧 |
| 供应链 | chain_analysis.py (512) + chain_engine.py (539) | — | 未迁移 |
| 配置 | config.py (124行, Python dict) | config/*.yaml (4文件) | 双配置源 |
| 数据获取 | data_fetcher.py (155行) | data/manager.py + 4源 (423行) | 重复 |
| 轮动 | rotation_detector.py (120行) | — | 未迁移 |
| 宏观 | eco_monitor.py (545行) + money_bond_kb.py (166行) | — | 未迁移 |
| 决策 | portfolio.py (225行) + strategy_optimizer.py (654行) | decision/screener.py (214行) | 重复 |
| 监控 | daily_patrol.py (349行) + deep_monitor.py (370行) | — | 未迁移 |

### 文件统计
- etf_system/: **51个.py文件, ~11,150行代码**
- etf-platform/: **27个.py文件, ~1,798行代码**
- 文档: **5个.md, ~57KB**
- 总计: **~78文件, ~13,000行**

---

## 核心问题

1. **两套配置** — config.py (dict) vs config/*.yaml，手动同步
2. **功能重复** — data_fetcher vs data/manager，portfolio vs screener
3. **入口分裂** — etf_system/run.py vs etf CLI
4. **平铺结构** — etf_system/下51个文件不分目录
5. **没有"全链路"命令** — 要跑穿透+轮动+宏观+建议需要串多个命令
6. **Python环境不稳定** — Hermes _internal目录中的python311.dll污染路径

---

## 整合方案

### 阶段1: 稳定基础设施 (1天)

#### 1.1 创建 python_path_fix.py
```python
"""在每个etf命令前自动清理path污染"""
import sys, os
sys.path = [p for p in sys.path if "_internal" not in p]
if "PYTHONPATH" in os.environ:
    parts = os.environ["PYTHONPATH"].split(os.pathsep)
    os.environ["PYTHONPATH"] = os.pathsep.join(p for p in parts if "_internal" not in p)
```

#### 1.2 合并配置源
修改etf-platform的config_loader.py，让它：
1. 优先读YAML配置
2. 如果YAML中没有某个ETF，fallback到旧的config.py
3. 旧的config.py通过桥接模块访问

### 阶段2: 迁移核心模块 (3天)

按优先级迁移etf_system/的功能到etf-platform/：

```
src/etf_platform/
├── analysis/                # NEW: 分析模块
│   ├── __init__.py
│   ├── penetration.py      # ← 从v7+v9合并 (核心: L1-L9评分+权重)
│   ├── chain.py            # ← 从chain_analysis合并 (依赖图谱+连锁故障)
│   ├── rotation.py         # ← 从rotation_detector合并 (行业轮动)
│   ├── macro.py            # ← 从eco_monitor合并 (宏观环境监控)
│   ├── demand.py           # ← 从demand_layer合并 (L10-L11需求)
│   └── signals.py          # ← 从signals/profit_signals合并
├── optimize/               # NEW: 策略优化
│   ├── __init__.py
│   ├── portfolio.py        # ← 从portfolio.py合并
│   ├── backtest.py         # ← 从backtest/simulate合并
│   └── strategy.py         # ← 从strategy_optimizer合并
├── data/                   # 已有 (扩展)
│   └── etf_system_bridge.py # NEW: 桥接旧版数据
├── decision/               # 已有
│   └── screener.py         # 扩展: 接入rotation/macro信号
├── pipeline.py             # 增强: 统一管道
├── cli.py                  # 增强: 新命令
├── analyst.py              # NEW: 统一分析师入口
└── advisor.py              # NEW: 投资建议引擎
```

### 阶段3: 统一CLI设计

```
etf run <CODE>               # 11层穿透 (已有)
etf report <CODE>            # 格式化报告 (已有)
etf analyse <CODE>           # 全面分析 (穿透+轮动+宏观+建议) ← NEW
etf screen [--top=N]         # 全市场扫描+排名 (已有)
etf compare <A> <B>          # 对比 (已有)
etf patrol                   # 每日巡逻 ← NEW
etf portfolio [--rebalance]  # 组合管理/再平衡 ← NEW
etf backtest <strategy>      # 策略回测 ← NEW
etf status                   # 系统状态+数据源健康 ← NEW
etf price <CODE>             # 实时行情 (已有)
etf news [keyword]           # 新闻 (已有)
etf valu <CODE>              # 估值 (已有)
```

### 阶段4: "全链路分析师"入口 design

analyst.py 封装完整的决策流程：

```python
def analyse(code, profile="均衡", live=True):
    """单只ETF全链路分析"""
    # 1. 穿透评分
    scores = run_penetration(code, live)
    # 2. 轮动信号 (该行业在轮动中的位置)
    rotation = detect_rotation(code)
    # 3. 宏观环境匹配度
    macro = check_macro_environment()
    # 4. 催化剂检测
    catalysts = check_catalysts(code)
    # 5. 综合建议
    advice = synthesize(scores, rotation, macro, catalysts, profile)
    return {
        "scores": scores, "rotation": rotation,
        "macro": macro, "catalysts": catalysts,
        "advice": advice, "profile": profile
    }

def patrol():
    """每日巡逻：全市场扫描+轮动+预警"""
    # 1. 数据源健康检查
    # 2. 全市场扫描(top N)
    # 3. 行业轮动变化
    # 4. 宏观指标变更
    # 5. 预警信号
    # 6. 投资建议
```

---

## 关键设计决策

### 为什么分析/目录而不是继续平铺
- 现有51个文件在一个目录，模块间循环引用风险高
- 按功能分包后，每个包可以独立测试、独立演进
- 新功能可以加在对应包里，不会让根目录膨胀

### 旧代码处理策略
- **不重写**旧的etf_system/，只通过适配器调用
- 迁移一个功能后，旧文件标记为`_deprecated_xxx.py`
- 等所有功能迁移完成再清理旧文件

### 数据流
```
数据源 (新浪/东财/akshare)
    ↓
data/manager (自动fallback)
    ↓
pipeline.py (统一管道)
    ↓
analysis/* → analyst.py → advice
decision/screener → screening results
optimize/* → portfolio/backtest
```

---

## 文件清单

### 需要创建
| 文件 | 预计行数 | 来源 |
|------|:-------:|------|
| src/etf_platform/path_fix.py | 20 | 新建 - path污染修复 |
| src/etf_platform/analyst.py | 200 | 新建 - 统一分析师入口 |
| src/etf_platform/advisor.py | 150 | 新建 - 投资建议引擎 |
| src/etf_platform/analysis/__init__.py | 5 | 新建 |
| src/etf_platform/analysis/penetration.py | 200 | 从v7+v9迁移核心 |
| src/etf_platform/analysis/chain.py | 150 | 从chain_analysis迁移核心 |
| src/etf_platform/analysis/rotation.py | 120 | 从rotation_detector迁移 |
| src/etf_platform/analysis/macro.py | 150 | 从eco_monitor迁移 |
| src/etf_platform/analysis/demand.py | 80 | 从demand_layer迁移 |
| src/etf_platform/analysis/signals.py | 100 | 从signals迁移 |
| src/etf_platform/optimize/__init__.py | 5 | 新建 |
| src/etf_platform/optimize/portfolio.py | 150 | 从portfolio迁移 |
| src/etf_platform/optimize/backtest.py | 120 | 从backtest+simulate迁移 |
| src/etf_platform/optimize/strategy.py | 200 | 从strategy_optimizer迁移核心 |
| src/etf_platform/data/etf_system_bridge.py | 80 | 新建 - 桥接旧版 |
| **小计** | **~1,730** | |

### 需要修改
| 文件 | 改动 |
|------|------|
| src/etf_platform/cli.py | 加新命令 analyse/patrol/portfolio/backtest/status |
| src/etf_platform/pipeline.py | 扩展run_full加入轮动+宏观维度 |
| src/etf_platform/decision/screener.py | 接入rotation/macro信号改进排序 |
| src/etf_platform/config_loader.py | fallback到旧config.py |
| pyproject.toml | 更新dependencies |

---

## 预期结果

整合完成后，用户只需一条命令就能完成分析：
```bash
etf analyse 512890     # 全链路分析一只ETF
etf screen --top=10    # 全市场扫描
etf patrol             # 每日巡逻（一键生成投资简报）
```

无需再手工跑 penetration_v9.py → rotation_detector.py → eco_monitor.py 多个脚本。